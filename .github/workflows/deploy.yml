name: Deploy Lab Infrastructure (Suricata on Host)

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H "${{ secrets.SSH_HOST }}" >> ~/.ssh/known_hosts

      - name: Cleanup previous deployment
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
        run: |
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no "${SSH_USER}@${SSH_HOST}" << 'EOF'
            set -e
            echo "=== Очистка предыдущего деплоя ==="

            # Останавливаем Suricata на хосте
            echo "Остановка Suricata..."
            sudo pkill -f "suricata.*-c /etc/suricata/suricata.yaml" 2>/dev/null || true
            sleep 3
            sudo rm -f /var/run/suricata.pid 2>/dev/null || true

            # Останавливаем и удаляем контейнеры
            cd ~ || mkdir -p ~/lab-infrastructure && cd ~/lab-infrastructure
            if [ -f docker-compose.yml ]; then
              docker compose down -v 2>/dev/null || true
            fi

            # Удаляем контейнеры по имени (на случай сбоя compose)
            docker rm -f attacker victim 2>/dev/null || true

            # Удаляем сеть (если существует)
            docker network rm labnet 2>/dev/null || true
            docker network rm lab-infrastructure_labnet 2>/dev/null || true

            # Очищаем папку
            rm -rf ~/lab-infrastructure
            mkdir -p ~/lab-infrastructure/{rules,logs}

            echo "Очистка завершена"
          EOF

      - name: Full Suricata & iptables Cleanup
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
        run: |
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no "${SSH_USER}@${SSH_HOST}" << 'EOF'
          set -e
          echo "=== Полная очистка Suricata и iptables ==="

          # 1. Убиваем ВСЕ процессы Suricata (не только по конфигу!)
          echo "→ Остановка всех Suricata..."
          sudo pkill -9 -f suricata 2>/dev/null || true
          sleep 2

          # 2. Удаляем PID и логи
          echo "→ Удаление PID и логов..."
          sudo rm -f /var/run/suricata.pid
          sudo rm -rf /var/log/suricata/*
          sudo mkdir -p /var/log/suricata

          # 3. Удаляем конфиги и правила
          echo "→ Удаление конфигов..."
          sudo rm -f /etc/suricata/suricata.yaml
          sudo rm -rf /etc/suricata/rules/*
          sudo mkdir -p /etc/suricata/rules

          # 4. Полная очистка iptables (удаляем ВСЁ, связанное с Suricata)
          echo "→ Очистка iptables..."
          # Удаляем ссылки
          while sudo iptables -C DOCKER-USER -j PRE-SURICATA 2>/dev/null; do
            sudo iptables -D DOCKER-USER -j PRE-SURICATA
          done
          # Очищаем и удаляем цепочку
          sudo iptables -F PRE-SURICATA 2>/dev/null || true
          sudo iptables -X PRE-SURICATA 2>/dev/null || true
          # Восстанавливаем DOCKER-USER по умолчанию (если нужно)
          sudo iptables -F DOCKER-USER 2>/dev/null || true
          sudo iptables -X DOCKER-USER 2>/dev/null || true
          sudo iptables -N DOCKER-USER 2>/dev/null || true

          # 5. Сбрасываем nfqueue (принудительно освобождаем ядро)
          echo "→ Сброс nfqueue..."
          echo 0 | sudo tee /proc/sys/net/netfilter/nf_queue_maxlen 2>/dev/null || true
          # Перезагружаем модуль (если возможно)
          sudo rmmod nfnetlink_queue 2>/dev/null || true
          sudo modprobe nfnetlink_queue 2>/dev/null || true

          echo "✅ Очистка завершена"
          exit 0
          EOF

      - name: Copy files to server
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
        run: |
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no "${SSH_USER}@${SSH_HOST}" "mkdir -p ~/lab-infrastructure/{rules,logs,feeds}"

          scp -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no \
            docker-compose.yml \
            suricata.yaml \
            fetch_feeds.sh \
            generate_custom_ioc_rules.py \
            update_rules.sh \
            setup-iptables.sh \
            setup-nfqueue.sh \
            start-suricata-ips.sh \
            Makefile \
            ${SSH_USER}@${SSH_HOST}:~/lab-infrastructure/
          
          # Делаем скрипты исполняемыми
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no ${SSH_USER}@${SSH_HOST} \
            "chmod +x ~/lab-infrastructure/setup-iptables.sh ~/lab-infrastructure/setup-nfqueue.sh ~/lab-infrastructure/start-suricata-ips.sh"

          scp -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no \
            -r rules/ \
            ${SSH_USER}@${SSH_HOST}:~/lab-infrastructure/

      - name: Install and configure Suricata on host
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
        run: |
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no "${SSH_USER}@${SSH_HOST}" << 'EOF'
            set -e
            echo "=== Установка Suricata и зависимостей ==="

            # Обновляем пакеты и устанавливаем Suricata
            sudo apt-get update
            sudo apt-get install -y software-properties-common curl python3 python3-pip
            sudo add-apt-repository -y ppa:oisf/suricata-stable
            sudo apt-get update
            
            # В Suricata 8.x suricata-update уже включен в основной пакет
            # Устанавливаем только suricata (suricata-update будет включен автоматически)
            sudo apt-get install -y suricata

            # Устанавливаем опциональные зависимости
            sudo apt-get install -y jq || echo "⚠️ jq не установлен (не критично)"
            
            # Проверяем наличие suricata-update
            if command -v suricata-update >/dev/null 2>&1; then
              echo "✅ suricata-update доступен: $(suricata-update --version 2>/dev/null || echo 'версия неизвестна')"
            else
              echo "⚠️ suricata-update не найден в PATH, но может быть доступен через suricata"
            fi

            # Устанавливаем права на выполнение для скриптов
            cd ~/lab-infrastructure
            chmod +x fetch_feeds.sh generate_custom_ioc_rules.py update_rules.sh

            # Подготавливаем конфиги
            sudo mkdir -p /etc/suricata/rules /var/log/suricata
            sudo cp ~/lab-infrastructure/suricata.yaml /etc/suricata/suricata.yaml
            sudo cp ~/lab-infrastructure/rules/* /etc/suricata/rules/ 2>/dev/null || true

            echo "✅ Suricata и зависимости установлены"
          EOF

      - name: Update Suricata rules (official + custom IoC)
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
        run: |
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no "${SSH_USER}@${SSH_HOST}" << 'EOF'
            set -e
            echo "=== Обновление правил Suricata ==="
            cd ~/lab-infrastructure

            # Убеждаемся, что suricata.yaml существует перед любыми операциями
            if [ ! -f /etc/suricata/suricata.yaml ]; then
              echo "[*] Копирование suricata.yaml в /etc/suricata/..."
              sudo mkdir -p /etc/suricata/rules
              sudo cp ~/lab-infrastructure/suricata.yaml /etc/suricata/suricata.yaml
            fi

            # Обновляем индекс источников правил
            echo "[1/4] Обновление индекса источников..."
            sudo suricata-update update-sources || echo "⚠️ Не удалось обновить индекс (возможно, уже актуален)"

            # Включаем ET Open (если еще не включен)
            echo "[2/4] Включение ET Open rules..."
            if ! sudo suricata-update list-enabled-sources 2>/dev/null | grep -q "et/open"; then
              sudo suricata-update enable-source et/open || echo "⚠️ ET Open уже включен или недоступен"
            fi

            # Пробуем включить Positive Technologies rules
            echo "[3/4] Попытка включения PT Security rules..."
            # Ищем PT source более аккуратно - берем только строки, которые не содержат "Subscription" и другие служебные строки
            PT_SOURCE=$(sudo suricata-update list-sources 2>/dev/null | grep -iE "^[a-z0-9]+/[a-z0-9-]+.*pt" | head -1 | awk '{print $1}' || echo "")
            if [ -n "$PT_SOURCE" ] && [ "$PT_SOURCE" != "Subscription:" ]; then
              if ! sudo suricata-update list-enabled-sources 2>/dev/null | grep -q "$PT_SOURCE"; then
                sudo suricata-update enable-source "$PT_SOURCE" || echo "⚠️ Не удалось включить PT rules"
              else
                echo "ℹ️ PT source уже включен: $PT_SOURCE"
              fi
            else
              echo "ℹ️ PT Security source не найден в доступных источниках"
            fi

            # Загружаем официальные правила (suricata.yaml уже должен быть на месте)
            echo "[4/4] Загрузка официальных правил..."
            
            # Пробуем загрузить правила с отключением проблемных правил modbus
            echo "[*] Попытка загрузки правил с обработкой ошибок modbus..."
            if sudo suricata-update --no-reload 2>&1 | tee /tmp/suricata-update.log; then
              echo "✅ Правила загружены успешно"
            else
              # Если загрузка не удалась из-за modbus, пробуем отключить modbus правила
              echo "⚠️ Первая попытка загрузки не удалась, проверяем причину..."
              if grep -q "modbus" /tmp/suricata-update.log 2>/dev/null; then
                echo "[*] Обнаружена проблема с modbus правилами, отключаем их..."
                # Отключаем правила modbus через disable.conf (формат: regex для SID или сообщения)
                sudo mkdir -p /var/lib/suricata/update
                # Отключаем правила modbus по SID (2250007, 2250008, 2250009) и по протоколу
                {
                  echo "re:^alert modbus"
                  echo "re:modbus"
                  echo "2250007"
                  echo "2250008"
                  echo "2250009"
                } | sudo tee /var/lib/suricata/update/disable.conf >/dev/null 2>&1 || true
                echo "[*] Повторная попытка загрузки правил..."
                if sudo suricata-update --no-reload 2>&1 | tee /tmp/suricata-update-retry.log; then
                  echo "✅ Правила загружены после отключения modbus"
                else
                  echo "⚠️ Не удалось загрузить правила даже после отключения modbus"
                  echo "[*] Пробуем загрузить правила без проверки (--no-test)..."
                  sudo suricata-update --no-reload --no-test 2>&1 || echo "⚠️ Загрузка без проверки также не удалась"
                fi
              fi
            fi
            
            # Копируем правила, если они были загружены
            if [ -f /var/lib/suricata/rules/suricata.rules ]; then
              echo "[*] Копирование правил в /etc/suricata/rules/..."
              sudo cp /var/lib/suricata/rules/suricata.rules /etc/suricata/rules/suricata.rules
              echo "✅ Правила скопированы"
              
              # Подсчитываем количество правил ET Open
              echo ""
              echo "=== Статистика загруженных правил Emerging Threats Open ==="
              
              # Подсчитываем активные правила (alert, drop, pass)
              ACTIVE_RULES=$(sudo grep -E "^\s*(alert|drop|pass)" /etc/suricata/rules/suricata.rules 2>/dev/null | wc -l || echo "0")
              
              # Подсчитываем правила по типам
              ALERT_RULES=$(sudo grep -E "^\s*alert" /etc/suricata/rules/suricata.rules 2>/dev/null | wc -l || echo "0")
              DROP_RULES=$(sudo grep -E "^\s*drop" /etc/suricata/rules/suricata.rules 2>/dev/null | wc -l || echo "0")
              PASS_RULES=$(sudo grep -E "^\s*pass" /etc/suricata/rules/suricata.rules 2>/dev/null | wc -l || echo "0")
              
              echo "📊 Всего активных правил в suricata.rules (ET Open + PT Security): $ACTIVE_RULES"
              echo "   - alert правил: $ALERT_RULES"
              echo "   - drop правил: $DROP_RULES"
              echo "   - pass правил: $PASS_RULES"
              
              # Показываем информацию о включенных источниках
              echo ""
              echo "Включенные источники правил:"
              ENABLED_SOURCES=$(sudo suricata-update list-enabled-sources 2>/dev/null | grep -v "^$" || echo "")
              if [ ! -z "$ENABLED_SOURCES" ]; then
                echo "$ENABLED_SOURCES" | while read -r source; do
                  echo "  ✓ $source"
                done
              else
                echo "  ⚠️ Не удалось получить список источников"
              fi
              
              # Показываем примеры правил ET Open
              echo ""
              echo "Примеры правил ET Open (первые 3):"
              sudo grep -E "^\s*(alert|drop|pass)" /etc/suricata/rules/suricata.rules 2>/dev/null | head -3 | sed 's/^/  /' || echo "  Правила не найдены"
              echo ""
            else
              echo "⚠️ Файл правил не найден после загрузки"
              echo "Проверяем наличие suricata.yaml..."
              ls -la /etc/suricata/suricata.yaml || echo "❌ suricata.yaml не найден!"
              # Создаем пустой файл правил, чтобы Suricata не падала
              if [ ! -f /etc/suricata/rules/suricata.rules ]; then
                echo "[*] Создаем пустой файл правил для совместимости..."
                sudo touch /etc/suricata/rules/suricata.rules
                echo "# Empty rules file - official rules not loaded" | sudo tee /etc/suricata/rules/suricata.rules
              fi
            fi

            # Загружаем IoC-фиды и генерируем кастомные правила
            echo "[*] Загрузка IoC-фидов..."
            ./fetch_feeds.sh || echo "⚠️ Некоторые фиды не загружены"

            echo "[*] Генерация кастомных правил..."
            # Генерируем правила (скрипт сам определит правильный путь)
            cd ~/lab-infrastructure
            python3 generate_custom_ioc_rules.py || {
              echo "⚠️ Не удалось сгенерировать правила, используем существующие"
            }

            # Копируем сгенерированные правила в /etc/suricata/rules если они созданы локально
            if [ -f ~/lab-infrastructure/rules/custom_ioc.rules ]; then
              sudo cp ~/lab-infrastructure/rules/custom_ioc.rules /etc/suricata/rules/ 2>/dev/null || true
            fi

            # Проверяем наличие файлов правил
            echo "[*] Проверка файлов правил..."
            if [ ! -f /etc/suricata/rules/suricata.rules ]; then
              echo "⚠️ suricata.rules не найден, создаем пустой файл..."
              echo "# Empty rules file" | sudo tee /etc/suricata/rules/suricata.rules
            fi
            if [ ! -f /etc/suricata/rules/custom_ioc.rules ]; then
              echo "⚠️ custom_ioc.rules не найден!"
            fi
            
            # Проверяем конфигурацию
            echo "[*] Проверка конфигурации Suricata..."
            if sudo suricata -T -c /etc/suricata/suricata.yaml 2>&1 | tee /tmp/suricata-test.log; then
              echo ""
              echo "=== Итоговая статистика правил Suricata ==="
              # Извлекаем информацию о загруженных правилах из лога проверки
              if grep -q "rules successfully loaded" /tmp/suricata-test.log; then
                RULES_INFO=$(grep "rules successfully loaded" /tmp/suricata-test.log | tail -1)
                echo "✅ $RULES_INFO"
              fi
              if grep -q "signatures processed" /tmp/suricata-test.log; then
                SIG_INFO=$(grep "signatures processed" /tmp/suricata-test.log | tail -1)
                echo "📊 $SIG_INFO"
              fi
              # Показываем количество правил по файлам
              echo ""
              echo "Количество правил по файлам:"
              for rule_file in custom_ioc.rules custom.rules suricata.rules; do
                if [ -f "/etc/suricata/rules/$rule_file" ]; then
                  COUNT=$(sudo grep -vE "^\s*#|^\s*$" "/etc/suricata/rules/$rule_file" 2>/dev/null | grep -E "^\s*(alert|drop|pass)" | wc -l || echo "0")
                  echo "  - $rule_file: $COUNT правил"
                fi
              done
              echo ""
              echo "✅ Конфигурация валидна"
            else
              echo "❌ Ошибка в конфигурации!"
              echo "Последние строки лога:"
              tail -30 /tmp/suricata-test.log || true
              # Проверяем, есть ли критичные ошибки или только предупреждения
              if grep -q "No rule files match" /tmp/suricata-test.log; then
                echo "⚠️ Предупреждение: некоторые файлы правил не найдены, но это не критично"
                # Продолжаем, если это только предупреждение о файлах правил
              else
                exit 1
              fi
            fi

            echo "✅ Правила обновлены и проверены"
          EOF

      - name: Start Suricata in IPS mode (nfq)
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
        run: |
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no "${SSH_USER}@${SSH_HOST}" << 'EOF'
          set -e
          echo "=== Запуск Suricata в IPS-режиме ==="

          sudo pkill -9 -f suricata 2>/dev/null || true
          sudo rm -f /var/run/suricata.pid 2>/dev/null || true
          sleep 1

          echo "Запуск Suricata с принудительной загрузкой правил..."
          setsid nohup sudo -b sh -c '
            exec > /var/log/suricata/startup.log 2>&1
            exec suricata \
              -c /etc/suricata/suricata.yaml \
              -q 0 \
              --pidfile /var/run/suricata.pid \
              --set logging.console.enabled=no
          ' </dev/null >/dev/null 2>&1 &

          # Ждём PID и инициализацию
          for i in {1..20}; do
            if [ -f /var/run/suricata.pid ]; then
              PID=$(cat /var/run/suricata.pid)
              echo "PID: $PID"
              break
            fi
            sleep 0.5
          done

          if [ ! -f /var/run/suricata.pid ]; then
            echo "❌ PID не создан"
            cat /var/log/suricata/startup.log 2>/dev/null || echo "(лог пуст)"
            exit 1
          fi

          # Ждём загрузки правил (появления alert/drop в логе)
          timeout 20 bash -c '
            while ! sudo grep -q "rules successfully loaded" /var/log/suricata/suricata.log &&
                  ! sudo grep -q "\"rules_loaded\":[1-9]" /var/log/suricata/eve.json 2>/dev/null; do
              kill -0 "$PID" || exit 1
              sleep 0.5
            done
          ' || {
            echo "❌ Правила не загружены"
            sudo tail -n 50 /var/log/suricata/suricata.log
            exit 1
          }

          echo "✅ Suricata запущена и загрузила правила"
          exit 0
          EOF

      - name: Deploy containers
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
        run: |
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no "${SSH_USER}@${SSH_HOST}" << 'EOF'
            set -e
            cd ~/lab-infrastructure

            docker network create labnet 2>/dev/null || true

            docker compose up -d --build

            echo "Контейнеры запущены:"
            docker compose ps
            
            # Ждем, пока контейнеры полностью инициализируются
            echo "Ожидание инициализации контейнеров..."
            sleep 10
            
            # Проверяем, что HTTP сервер запустился
            echo "Проверка готовности HTTP сервера..."
            for i in {1..30}; do
              if docker exec victim ps aux 2>/dev/null | grep -q "http.server"; then
                echo "HTTP сервер запущен (попытка $i/30)"
                break
              fi
              if [ $i -eq 30 ]; then
                echo "⚠️ HTTP сервер не запустился за 30 секунд"
                docker exec victim ps aux | head -10
              fi
              sleep 1
            done
          EOF

      - name: Configure iptables for Suricata IPS
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
        run: |
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no "${SSH_USER}@${SSH_HOST}" << 'EOF'
          set -e
          echo "=== Настройка iptables для Suricata IPS ==="

          # Получаем имя bridge-интерфейса
          BRIDGE_NAME="br-$(docker network inspect lab-infrastructure_labnet -f '{{.Id}}' | cut -c1-12)"
          echo "Bridge: $BRIDGE_NAME"

          if ! ip link show "$BRIDGE_NAME" >/dev/null 2>&1; then
            echo "❌ Bridge $BRIDGE_NAME не найден. Список:"
            ip -br link show type bridge
            exit 1
          fi

          # --- Безопасное создание цепочки: удаляем ВСЁ, что связано с PRE-SURICATA ---
          echo "Очистка старых правил..."

          # Удаляем ссылки из DOCKER-USER (может быть несколько — удаляем в цикле)
          while sudo iptables -C DOCKER-USER -j PRE-SURICATA 2>/dev/null; do
            sudo iptables -D DOCKER-USER -j PRE-SURICATA
          done

          # Очищаем и удаляем цепочку, если существует
          sudo iptables -F PRE-SURICATA 2>/dev/null || true
          sudo iptables -X PRE-SURICATA 2>/dev/null || true

          # Создаём заново
          echo "Создание цепочки PRE-SURICATA..."
          sudo iptables -N PRE-SURICATA
          sudo iptables -A DOCKER-USER -j PRE-SURICATA

          # Добавляем правила для bridge
          sudo iptables -A PRE-SURICATA -i "$BRIDGE_NAME" -j NFQUEUE --queue-num 0 --queue-bypass
          sudo iptables -A PRE-SURICATA -o "$BRIDGE_NAME" -j NFQUEUE --queue-num 0 --queue-bypass

          echo "✅ iptables настроены"
          sudo iptables -L DOCKER-USER -n -v
          exit 0
          EOF

      - name: Verify deployment
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
        run: |
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no "${SSH_USER}@${SSH_HOST}" << 'EOF'
            set -e
            cd ~/lab-infrastructure

            echo "=== Статус контейнеров ==="
            docker compose ps

            echo ""
            echo "=== Статус Suricata (PID/процесс) ==="
            if [ -f /var/run/suricata.pid ]; then
              PID=$(cat /var/run/suricata.pid)
              if kill -0 "$PID" 2>/dev/null; then
                echo "✅ Suricata работает (PID: $PID)"
              else
                echo "❌ Процесс Suricata завершён"
                exit 1
              fi
            else
              echo "❌ PID-файл не найден"
              exit 1
            fi

            echo ""
            echo "=== Правила iptables ==="
            sudo iptables -L DOCKER-USER -n -v

            echo ""
            echo "=== Последние 20 событий Suricata ==="
            sudo tail -n 20 /var/log/suricata/eve.json 2>/dev/null | jq -r .event_type // . 2>/dev/null || echo "(логи пусты или отсутствуют)"
          EOF

      - name: Test Suricata IPS protection
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
        run: |
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no "${SSH_USER}@${SSH_HOST}" << 'EOF'
            set -e
            cd ~/lab-infrastructure

            echo "=== Тестирование: ICMP должен блокироваться, HTTP — проходить ==="
            sleep 5

            echo "→ Тест ICMP (ожидается блокировка):"
            # Проверяем наличие правила для ICMP
            if [ -f /etc/suricata/rules/custom.rules ]; then
              echo "  Правило custom.rules найдено"
              sudo grep -i icmp /etc/suricata/rules/custom.rules || echo "  ⚠️ Правило ICMP не найдено в файле"
            else
              echo "  ⚠️ Файл custom.rules не найден!"
            fi
            
            # Пытаемся отправить ICMP пакет
            if docker exec attacker timeout 3 ping -c 1 -W 1 victim &>/dev/null; then
              echo "❌ ICMP прошёл — Suricata НЕ блокирует!"
              echo "  Диагностика:"
              echo "  Последние события Suricata (ICMP/drop):"
              sudo tail -n 30 /var/log/suricata/eve.json 2>/dev/null | grep -E '"icmp"|"drop"|"alert"' | tail -5 | jq -r '.event_type, .alert.signature, .src_ip, .dest_ip' 2>/dev/null || echo "  Нет событий ICMP/drop"
              echo "  Проверка правил ICMP:"
              sudo grep -i icmp /etc/suricata/rules/*.rules 2>/dev/null | head -5 || echo "  Правила ICMP не найдены"
              echo "  Статус Suricata:"
              if [ -f /var/run/suricata.pid ]; then
                PID=$(cat /var/run/suricata.pid)
                if kill -0 "$PID" 2>/dev/null; then
                  echo "  Suricata работает (PID: $PID)"
                else
                  echo "  ⚠️ Процесс Suricata не найден"
                fi
              else
                echo "  ⚠️ PID файл не найден"
              fi
              exit 1
            else
              echo "✅ ICMP заблокирован"
            fi

            echo "→ Тест HTTP (ожидается разрешение):"
            # Ждем, пока HTTP сервер точно запустится (контейнер может еще устанавливать пакеты)
            echo "  Ожидание запуска HTTP сервера..."
            for i in {1..30}; do
              if docker exec victim ps aux 2>/dev/null | grep -q "http.server"; then
                echo "  HTTP сервер запущен (попытка $i/30)"
                break
              fi
              if [ $i -eq 30 ]; then
                echo "  ⚠️ HTTP сервер не запустился за 30 секунд"
                docker exec victim ps aux | head -10
              fi
              sleep 1
            done
            sleep 2  # Дополнительная пауза для полной инициализации
            
            # Проверяем доступность HTTP сервера
            if docker exec attacker timeout 5 curl -s --connect-timeout 3 http://victim &>/dev/null; then
              echo "✅ HTTP прошёл"
              echo "→ Проверяем, было ли событие в Suricata (должно быть, если правило есть):"
              if sudo grep -q '"icmp"' /var/log/suricata/eve.json 2>/dev/null; then
                echo "⚠️ ICMP-событие зафиксировано (ожидаемо)"
              fi
            else
              echo "❌ HTTP не прошёл"
              echo "→ Диагностика:"
              echo "  Проверка статуса victim контейнера:"
              docker exec victim ps aux | grep -E "python|http.server" || echo "  HTTP сервер не найден в процессах"
              echo "  Проверка доступности порта 80:"
              docker exec attacker timeout 2 nc -zv victim 80 2>&1 || echo "  Порт 80 недоступен"
              echo "  Последние события Suricata (HTTP/drop):"
              sudo tail -n 50 /var/log/suricata/eve.json 2>/dev/null | grep -E '"http"|"drop"|"alert"' | tail -10 | jq -r 'select(.event_type=="drop" or .event_type=="alert") | "\(.event_type) | src:\(.src_ip // "N/A") | dst:\(.dest_ip // "N/A") | sig:\(.alert.signature // "N/A")"' 2>/dev/null || echo "  Нет событий HTTP/drop"
              echo "  IP адрес attacker контейнера:"
              ATTACKER_IP=$(docker inspect attacker 2>/dev/null | jq -r '.[0].NetworkSettings.Networks.labnet.IPAddress // .[0].NetworkSettings.Networks.lab-infrastructure_labnet.IPAddress // empty')
              if [ -n "$ATTACKER_IP" ]; then
                echo "  $ATTACKER_IP"
              else
                docker inspect attacker 2>/dev/null | grep -i ipaddress | head -1 || echo "  Не удалось определить IP"
              fi
              echo "  IP адрес victim контейнера:"
              VICTIM_IP=$(docker inspect victim 2>/dev/null | jq -r '.[0].NetworkSettings.Networks.labnet.IPAddress // .[0].NetworkSettings.Networks.lab-infrastructure_labnet.IPAddress // empty')
              if [ -n "$VICTIM_IP" ]; then
                echo "  $VICTIM_IP"
              else
                docker inspect victim 2>/dev/null | grep -i ipaddress | head -1 || echo "  Не удалось определить IP"
              fi
              echo "  Попытка прямого подключения к victim:"
              docker exec attacker timeout 3 curl -v http://victim 2>&1 | head -20 || echo "  Подключение не удалось"
              echo "  Проверка правил pass для HTTP:"
              sudo grep -E "^pass.*http|^pass.*80" /etc/suricata/rules/custom_ioc.rules 2>/dev/null | head -3 || echo "  Правила pass не найдены"
              echo "  Проверка правил drop для HTTP:"
              sudo grep -E "^drop.*http|^drop.*80" /etc/suricata/rules/custom_ioc.rules 2>/dev/null | head -3 || echo "  Правила drop для HTTP не найдены"
              exit 1
            fi

            echo "=== 🎉 Тест пройден успешно! ==="
          EOF

      - name: Test IoC rules blocking
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
        run: |
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no "${SSH_USER}@${SSH_HOST}" << 'EOF'
            set -e
            cd ~/lab-infrastructure

            echo "=== Тестирование блокировки по IoC правилам ==="
            sleep 3

            # Собираем список IP-адресов из разных заблокированных источников для теста
            echo "→ Сбор тестовых IP-адресов из IoC-фидов..."
            
            # Используем временный файл для хранения списка IP (более надежно в bash)
            TEST_IPS_FILE=$(mktemp)
            
            # Получаем IP из разных источников (максимум 1-2 IP из каждого источника)
            if [ -f ~/lab-infrastructure/feeds/feodo_ips.txt ]; then
              IP=$(head -1 ~/lab-infrastructure/feeds/feodo_ips.txt 2>/dev/null | grep -oE '([0-9]{1,3}\.){3}[0-9]{1,3}' | head -1)
              [ ! -z "$IP" ] && echo "$IP:Feodo Tracker" >> "$TEST_IPS_FILE"
            fi
            
            if [ -f ~/lab-infrastructure/feeds/urlhaus_ips.txt ]; then
              IP=$(head -1 ~/lab-infrastructure/feeds/urlhaus_ips.txt 2>/dev/null | grep -oE '([0-9]{1,3}\.){3}[0-9]{1,3}' | head -1)
              [ ! -z "$IP" ] && echo "$IP:URLhaus" >> "$TEST_IPS_FILE"
            fi
            
            if [ -f ~/lab-infrastructure/feeds/botvrij_ips.txt ]; then
              IP=$(head -1 ~/lab-infrastructure/feeds/botvrij_ips.txt 2>/dev/null | grep -oE '([0-9]{1,3}\.){3}[0-9]{1,3}' | head -1)
              [ ! -z "$IP" ] && echo "$IP:Botvrij.eu" >> "$TEST_IPS_FILE"
            fi
            
            if [ -f ~/lab-infrastructure/feeds/google_cloud_ips.txt ]; then
              IP=$(head -1 ~/lab-infrastructure/feeds/google_cloud_ips.txt 2>/dev/null | cut -d'/' -f1 | head -1)
              [ ! -z "$IP" ] && echo "$IP:Google Cloud" >> "$TEST_IPS_FILE"
              # Добавляем еще один IP из Google Cloud
              IP2=$(sed -n '2p' ~/lab-infrastructure/feeds/google_cloud_ips.txt 2>/dev/null | cut -d'/' -f1 | head -1)
              [ ! -z "$IP2" ] && echo "$IP2:Google Cloud" >> "$TEST_IPS_FILE"
            fi
            
            if [ -f ~/lab-infrastructure/feeds/aws_ips.txt ]; then
              IP=$(head -1 ~/lab-infrastructure/feeds/aws_ips.txt 2>/dev/null | cut -d'/' -f1 | head -1)
              [ ! -z "$IP" ] && echo "$IP:AWS" >> "$TEST_IPS_FILE"
            fi
            
            if [ -f ~/lab-infrastructure/feeds/azure_ips.txt ]; then
              IP=$(head -1 ~/lab-infrastructure/feeds/azure_ips.txt 2>/dev/null | cut -d'/' -f1 | head -1)
              [ ! -z "$IP" ] && echo "$IP:Azure" >> "$TEST_IPS_FILE"
            fi
            
            if [ -f ~/lab-infrastructure/feeds/cloudflare_ips.txt ]; then
              IP=$(head -1 ~/lab-infrastructure/feeds/cloudflare_ips.txt 2>/dev/null | cut -d'/' -f1 | head -1)
              [ ! -z "$IP" ] && echo "$IP:Cloudflare" >> "$TEST_IPS_FILE"
            fi
            
            if [ -f ~/lab-infrastructure/feeds/digitalocean_ips.txt ]; then
              IP=$(head -1 ~/lab-infrastructure/feeds/digitalocean_ips.txt 2>/dev/null | cut -d'/' -f1 | head -1)
              [ ! -z "$IP" ] && echo "$IP:DigitalOcean" >> "$TEST_IPS_FILE"
            fi
            
            if [ -f ~/lab-infrastructure/feeds/antifilter_ips.txt ]; then
              IP=$(head -1 ~/lab-infrastructure/feeds/antifilter_ips.txt 2>/dev/null | grep -oE '([0-9]{1,3}\.){3}[0-9]{1,3}' | head -1)
              [ ! -z "$IP" ] && echo "$IP:Antifilter" >> "$TEST_IPS_FILE"
            fi

            # Если не хватает IP, добавляем известные тестовые заблокированные IP
            IP_COUNT=$(wc -l < "$TEST_IPS_FILE" 2>/dev/null || echo "0")
            while [ "$IP_COUNT" -lt 10 ]; do
              case "$IP_COUNT" in
                9) echo "8.8.8.8:Google DNS (тестовый)" >> "$TEST_IPS_FILE" ;;
                *) break ;;
              esac
              IP_COUNT=$(wc -l < "$TEST_IPS_FILE" 2>/dev/null || echo "0")
            done

            TOTAL_IPS=$(wc -l < "$TEST_IPS_FILE" 2>/dev/null || echo "0")
            echo "Найдено тестовых IP-адресов: $TOTAL_IPS"
            echo ""

            # Тестируем блокировку каждого IP
            BLOCKED_COUNT=0
            PASSED_COUNT=0
            TOTAL_TESTED=0

            while IFS= read -r ip_info; do
              [ -z "$ip_info" ] && continue
              IP=$(echo "$ip_info" | cut -d':' -f1)
              SOURCE=$(echo "$ip_info" | cut -d':' -f2-)
              
              if [ -z "$IP" ] || [ "$IP" = "$SOURCE" ]; then
                continue
              fi

              TOTAL_TESTED=$((TOTAL_TESTED + 1))
              echo "→ Тест $TOTAL_TESTED: Блокировка IP $IP ($SOURCE)"
              
              # Пытаемся отправить пакет к заблокированному IP
              if docker exec attacker timeout 2 ping -c 1 -W 1 "$IP" &>/dev/null 2>&1; then
                echo "  ⚠️ Трафик к $IP прошел (может быть нормально, если IP не в HOME_NET или правило не сработало)"
                PASSED_COUNT=$((PASSED_COUNT + 1))
              else
                echo "  ✅ Трафик к $IP заблокирован или недоступен"
                BLOCKED_COUNT=$((BLOCKED_COUNT + 1))
              fi
              
              # Проверяем, есть ли правило для этого IP в custom_ioc.rules
              if [ -f /etc/suricata/rules/custom_ioc.rules ]; then
                if sudo grep -q "$IP" /etc/suricata/rules/custom_ioc.rules 2>/dev/null; then
                  RULE_LINE=$(sudo grep "$IP" /etc/suricata/rules/custom_ioc.rules 2>/dev/null | head -1)
                  echo "  ✓ Правило для $IP найдено в custom_ioc.rules"
                  echo "    $(echo "$RULE_LINE" | sed 's/^[[:space:]]*//' | cut -c1-80)"
                else
                  echo "  ⚠️ Правило для $IP не найдено в custom_ioc.rules"
                fi
              fi
              
              # Проверяем логи Suricata на наличие событий блокировки для этого IP
              sleep 1
              if sudo grep -q "$IP" /var/log/suricata/eve.json 2>/dev/null; then
                EVENT_COUNT=$(sudo grep -c "$IP" /var/log/suricata/eve.json 2>/dev/null || echo "0")
                echo "  📊 Найдено событий в логах для $IP: $EVENT_COUNT"
                if command -v jq >/dev/null 2>&1; then
                  LAST_EVENT=$(sudo grep "$IP" /var/log/suricata/eve.json 2>/dev/null | \
                    jq -R 'fromjson? | select(.dest_ip == "'"$IP"'" or .src_ip == "'"$IP"'")' 2>/dev/null | \
                    jq -s '.' | \
                    jq '.[-1]')
                  if [ ! -z "$LAST_EVENT" ] && [ "$LAST_EVENT" != "null" ]; then
                    echo "$LAST_EVENT" | jq -r '"    Тип: \(.event_type // "N/A") | Сигнатура: \(.alert.signature // .msg // "N/A") | SID: \(.alert.signature_id // "N/A")"' 2>/dev/null || true
                  fi
                fi
              else
                echo "  ℹ️ События для $IP не найдены в логах (может быть нормально, если трафик не проходил)"
              fi
              echo ""
            done < "$TEST_IPS_FILE"
            
            # Удаляем временный файл
            rm -f "$TEST_IPS_FILE"

            # Итоговая статистика тестирования
            echo "=========================================="
            echo "=== ИТОГИ ТЕСТИРОВАНИЯ БЛОКИРОВКИ IP ==="
            echo "=========================================="
            echo "Всего протестировано IP: $TOTAL_TESTED"
            echo "✅ Заблокировано/недоступно: $BLOCKED_COUNT"
            echo "⚠️ Прошло: $PASSED_COUNT"
            echo ""
            
            if [ "$BLOCKED_COUNT" -gt 0 ]; then
              echo "✅ Блокировка работает! Найдено $BLOCKED_COUNT заблокированных IP"
            else
              echo "⚠️ Блокировка не подтверждена. Возможные причины:"
              echo "   - IP-адреса не в HOME_NET (172.16.0.0/12)"
              echo "   - Правила блокировки не сработали"
              echo "   - Трафик не проходит через Suricata"
            fi
            echo ""

            # Проверяем статистику правил Suricata
            echo ""
            echo "→ Статистика правил Suricata:"
            if sudo suricatasc -c "ruleset-stats" 2>/dev/null | head -20; then
              echo "✅ Статистика правил получена"
            else
              echo "ℹ️ Не удалось получить статистику (suricatasc может быть недоступен)"
            fi

            # Проверяем количество загруженных правил
            echo ""
            echo "→ Проверка загруженных правил:"
            if [ -f /etc/suricata/rules/custom_ioc.rules ]; then
              RULE_COUNT=$(grep -c "^drop\|^pass" /etc/suricata/rules/custom_ioc.rules 2>/dev/null || echo "0")
              echo "  - custom_ioc.rules: $RULE_COUNT правил"
            fi
            if [ -f /etc/suricata/rules/suricata.rules ]; then
              RULE_COUNT=$(grep -c "^alert\|^drop\|^pass" /etc/suricata/rules/suricata.rules 2>/dev/null || echo "0")
              echo "  - suricata.rules: $RULE_COUNT правил"
            fi

            echo ""
            echo "=== ✅ Тестирование IoC правил завершено ==="
          EOF

      - name: Display Suricata EVE logs (blocking verification)
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
        run: |
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no "${SSH_USER}@${SSH_HOST}" << 'EOF'
            set -e
            cd ~/lab-infrastructure

            echo "=========================================="
            echo "=== ЛОГИ SURICATA (EVE.JSON) - ПОДТВЕРЖДЕНИЕ БЛОКИРОВКИ ==="
            echo "=========================================="
            echo ""

            # Проверяем наличие файла eve.json
            if [ ! -f /var/log/suricata/eve.json ]; then
              echo "❌ Файл /var/log/suricata/eve.json не найден!"
              echo "Проверяем директорию логов:"
              sudo ls -la /var/log/suricata/ 2>/dev/null || echo "Директория логов не найдена"
              exit 1
            fi

            # Проверяем размер файла
            EVE_SIZE=$(sudo wc -l < /var/log/suricata/eve.json 2>/dev/null || echo "0")
            if [ "$EVE_SIZE" -eq 0 ]; then
              echo "⚠️ Файл eve.json пуст. Suricata может не видеть трафик."
              echo "Проверяем статус Suricata:"
              if [ -f /var/run/suricata.pid ]; then
                PID=$(cat /var/run/suricata.pid)
                if kill -0 "$PID" 2>/dev/null; then
                  echo "  Suricata работает (PID: $PID)"
                else
                  echo "  ⚠️ Процесс Suricata не найден"
                fi
              fi
              exit 0
            fi

            echo "📊 Всего событий в eve.json: $EVE_SIZE"
            echo ""

            # Устанавливаем jq, если его нет
            if ! command -v jq >/dev/null 2>&1; then
              echo "Установка jq для форматирования JSON..."
              sudo apt-get update -qq && sudo apt-get install -y -qq jq || echo "Не удалось установить jq"
            fi

            # Выводим статистику событий
            echo "=== Статистика событий по типам ==="
            if command -v jq >/dev/null 2>&1; then
              echo "События drop (блокировки):"
              DROP_COUNT=$(sudo cat /var/log/suricata/eve.json 2>/dev/null | jq -R 'fromjson? | select(.event_type=="drop")' 2>/dev/null | wc -l || echo "0")
              echo "  Количество: $DROP_COUNT"
              
              echo ""
              echo "События alert (обнаружения):"
              ALERT_COUNT=$(sudo cat /var/log/suricata/eve.json 2>/dev/null | jq -R 'fromjson? | select(.event_type=="alert")' 2>/dev/null | wc -l || echo "0")
              echo "  Количество: $ALERT_COUNT"
              
              echo ""
              echo "HTTP события:"
              HTTP_COUNT=$(sudo cat /var/log/suricata/eve.json 2>/dev/null | jq -R 'fromjson? | select(.event_type=="http")' 2>/dev/null | wc -l || echo "0")
              echo "  Количество: $HTTP_COUNT"
              
              echo ""
              echo "ICMP события:"
              ICMP_COUNT=$(sudo cat /var/log/suricata/eve.json 2>/dev/null | jq -R 'fromjson? | select(.proto=="ICMP" or .proto=="IPv6-ICMP" or .icmp != null)' 2>/dev/null | wc -l || echo "0")
              echo "  Количество: $ICMP_COUNT"
            else
              echo "jq не установлен, используем базовый подсчет..."
              DROP_COUNT=$(sudo grep -c '"event_type":"drop"' /var/log/suricata/eve.json 2>/dev/null || echo "0")
              ALERT_COUNT=$(sudo grep -c '"event_type":"alert"' /var/log/suricata/eve.json 2>/dev/null || echo "0")
              HTTP_COUNT=$(sudo grep -c '"event_type":"http"' /var/log/suricata/eve.json 2>/dev/null || echo "0")
              echo "  drop: $DROP_COUNT, alert: $ALERT_COUNT, http: $HTTP_COUNT"
            fi
            echo ""

            # Выводим последние события drop (блокировки)
            echo "=== Последние события DROP (блокировки) - подтверждение работы IPS ==="
            if command -v jq >/dev/null 2>&1; then
              sudo cat /var/log/suricata/eve.json 2>/dev/null | \
                jq -R 'fromjson? | select(.event_type=="drop")' 2>/dev/null | \
                jq -s '.' | \
                jq '.[-10:]' | \
                jq -r '.[] | "\(.timestamp) | \(.event_type) | src:\(.src_ip // "N/A") | dst:\(.dest_ip // "N/A") | proto:\(.proto // "N/A") | sig:\(.alert.signature // "N/A")"' || \
                echo "События drop не найдены или файл пуст"
            else
              sudo grep '"event_type":"drop"' /var/log/suricata/eve.json 2>/dev/null | tail -10 || echo "События drop не найдены"
            fi
            echo ""

            # Выводим последние события alert
            echo "=== Последние события ALERT (обнаружения) ==="
            if command -v jq >/dev/null 2>&1; then
              sudo cat /var/log/suricata/eve.json 2>/dev/null | \
                jq -R 'fromjson? | select(.event_type=="alert")' 2>/dev/null | \
                jq -s '.' | \
                jq '.[-10:]' | \
                jq -r '.[] | "\(.timestamp) | \(.alert.signature // "N/A") | src:\(.src_ip // "N/A") | dst:\(.dest_ip // "N/A")"' || \
                echo "События alert не найдены"
            else
              sudo grep '"event_type":"alert"' /var/log/suricata/eve.json 2>/dev/null | tail -10 || echo "События alert не найдены"
            fi
            echo ""

            # Выводим последние HTTP события
            echo "=== Последние HTTP события (разрешенный трафик) ==="
            if command -v jq >/dev/null 2>&1; then
              sudo cat /var/log/suricata/eve.json 2>/dev/null | \
                jq -R 'fromjson? | select(.event_type=="http")' 2>/dev/null | \
                jq -s '.' | \
                jq '.[-5:]' | \
                jq -r '.[] | "\(.timestamp) | \(.http.hostname // "N/A") | \(.http.url // "N/A") | src:\(.src_ip // "N/A") | dst:\(.dest_ip // "N/A")"' || \
                echo "HTTP события не найдены"
            else
              sudo grep '"event_type":"http"' /var/log/suricata/eve.json 2>/dev/null | tail -5 || echo "HTTP события не найдены"
            fi
            echo ""

            # Выводим ICMP события (если есть)
            echo "=== ICMP события (должны быть заблокированы) ==="
            if command -v jq >/dev/null 2>&1; then
              ICMP_EVENTS=$(sudo cat /var/log/suricata/eve.json 2>/dev/null | \
                jq -R 'fromjson? | select(.proto=="ICMP" or .proto=="IPv6-ICMP" or .icmp != null)' 2>/dev/null | \
                jq -s '.' | \
                jq '.[-10:]')
              
              if [ ! -z "$ICMP_EVENTS" ] && [ "$ICMP_EVENTS" != "[]" ]; then
                echo "$ICMP_EVENTS" | jq -r '.[] | "\(.timestamp) | \(.event_type // "flow") | src:\(.src_ip // "N/A") | dst:\(.dest_ip // "N/A") | proto:\(.proto // "N/A")"' || echo "ICMP события не найдены"
              else
                echo "ICMP события не найдены (может быть нормально, если Suricata не видит трафик)"
              fi
            else
              sudo grep -E '"icmp"|"ICMP"' /var/log/suricata/eve.json 2>/dev/null | tail -10 || echo "ICMP события не найдены"
            fi
            echo ""

            # Итоговая сводка
            echo "=========================================="
            echo "=== ИТОГОВАЯ СВОДКА ==="
            echo "=========================================="
            echo "✅ Файл eve.json существует и содержит события"
            echo "📊 Всего событий: $EVE_SIZE"
            if [ "$DROP_COUNT" -gt 0 ]; then
              echo "✅ Найдено событий блокировки (drop): $DROP_COUNT"
              echo "   Это подтверждает, что Suricata в режиме IPS успешно блокирует трафик!"
            else
              echo "⚠️ События блокировки (drop) не найдены"
              echo "   Возможные причины:"
              echo "   - Suricata работает в режиме IDS (только обнаружение)"
              echo "   - Правила с действием drop не сработали"
              echo "   - Трафик не проходил через NFQUEUE"
            fi
            if [ "$HTTP_COUNT" -gt 0 ]; then
              echo "✅ Найдено HTTP событий: $HTTP_COUNT"
              echo "   Это подтверждает, что HTTP трафик разрешен и логируется"
            fi
            echo ""
            echo "📁 Полный путь к логам: /var/log/suricata/eve.json"
            echo "📁 Текстовые логи: /var/log/suricata/suricata.log"
            echo ""
            echo "=========================================="
          EOF